<% flash.each do |msg_type, message| %>
  <div class="alert <%= bootstrap_class_for(msg_type) %> mt-5">
    <div class="container">
      <button class="close" data-dismiss="alert"><span>x</span></btn>
      <%= message %>
    </div>
  </div>
<% end %>
